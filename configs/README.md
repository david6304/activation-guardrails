# Configs

Experiment settings belong here. Prefer small YAML files that make runs
reproducible: model id/revision, dataset id/revision, split seed, token position,
feature type, layer, metric, and threshold rule.

Do not put secrets, local absolute cache paths, or generated run outputs here.
