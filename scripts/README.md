# Scripts

Runnable entrypoints belong here. Keep scripts thin: parse arguments, load a
config, call `src/agguardrails/`, and write artifacts/results with metadata.

Avoid putting reusable experiment logic directly in scripts.
