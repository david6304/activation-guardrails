# Scripts

Future runnable experiment and cluster entrypoints belong here. Keep experiment
scripts thin: parse arguments, load a config, call reusable package code, and
write artifacts/results with metadata.

Avoid putting reusable experiment logic directly in scripts.
