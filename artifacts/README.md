# Artifacts

Local cached intermediates belong here: downloaded/processed activations, SAE
features, fitted probes, response caches, and other expensive-to-recreate files.

Artifacts are ignored by default. Reportable results should point back to their
artifact metadata, not require committing large binary files.
