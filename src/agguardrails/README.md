# agguardrails

Reusable library code belongs here: data schemas, metric functions, model
wrappers, activation extraction helpers, SAE encoding helpers, and probe
training utilities.

Keep it simple. Add abstractions only when at least two scripts need the same
logic or when testing is much clearer through a library function.
