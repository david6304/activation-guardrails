# Data

Raw, interim, and processed datasets live here when needed locally. Most data is
ignored because benchmark files can be large or gated.

Commit only small metadata, schemas, or synthetic fixtures. Do not commit gated
WildJailbreak data or generated harmful model outputs.
